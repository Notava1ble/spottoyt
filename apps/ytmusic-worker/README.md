# YouTube Music Worker

This package is reserved for the Python `ytmusicapi` adapter.

The first shell does not authenticate with YouTube Music yet. Future work should keep
all direct `ytmusicapi` calls inside this worker or behind the API `YtmusicService`.
